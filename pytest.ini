[pytest]
markers =
    interactive: requires viewer, GUI, camera, or manual inspection
    viewer: requires MuJoCo viewer and display
    mujoco: requires MuJoCo model loading
    slow: longer-running validation
